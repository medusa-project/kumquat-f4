<% provide :title, 'Collections' %>
<% provide :body_id, 'collections_index' %>
<% provide :active_nav, 'collections' %>

<div class="btn-group pull-right">
  <%= link_to new_admin_collection_path, class: 'btn btn-default' do %>
    <i class="fa fa-cubes"></i> Add Collection
  <% end %>
</div>

<h1>Collections <small><%= @collections.total_length %> total</small></h1>

<ul>
  <% @collections.each do |collection| %>
    <li class="clearfix">
      <%= link_to collection do %>
        <%= image_tag('', width: 100, height: 100) %>
      <% end %>
      <span class="title">
      <%= link_to collection.title, admin_collection_path(collection) %>
        <small><%= pluralize(collection.num_items, 'item') %></small>
      </span>
      <br>
      <span class="description">
        <%= truncate(collection.triple('http://purl.org/dc/elements/1.1/description'), length: 400) %>
      </span>
    </li>
  <% end %>
</ul>

<%= paginate(@collections, @limit, @current_page) %>
