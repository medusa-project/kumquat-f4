<!DOCTYPE html>
<html>
  <head>
    <title><%= yield :title %> |
      <%= Kumquat::Application.kumquat_config[:page_title] %></title>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <link href='http://fonts.googleapis.com/css?family=Oswald|PT+Sans'
          rel='stylesheet' type='text/css'>

    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
  <body <%= !yield(:body_id).empty? ? raw("id=\"#{yield(:body_id)}\"") : nil %>
        <%= raw("data-nav=\"#{yield(:active_nav)}\"") %>>

    <%= render 'layouts/header' %>

    <nav class="navbar navbar-default" role="navigation">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#kq-navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'Home', root_path, class: 'navbar-brand' %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="kq-navbar-collapse">
          <ul class="nav navbar-nav">
            <li id="collections-nav"><%= link_to 'Collections', collections_path %></li>
            <li class="dropdown" id="items-nav">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"
                 role="button" aria-expanded="false">Items<span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "All Items (#{@num_items})", items_path %></li>
                <li class="divider"></li>
                <li><%= link_to "Images (#{@num_images})", items_path(q: 'kq_system_media_type:image/*') %></li>
                <li><%= link_to "Videos (#{@num_videos})", items_path(q: 'kq_system_media_type:video/*') %></li>
                <li><%= link_to "Sound Recordings (#{@num_audios})", items_path(q: 'kq_system_media_type:audio/*') %></li>
              </ul>
            </li>
            <li id="favorites-nav">
              <%= link_to favorites_path do %>
                My Favorites <span class="badge"><%= num_favorites %></span>
              <% end %>
            </li>
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <form method="get" action="<%= items_path %>"
                  class="navbar-form navbar-right kq-quick-search" role="search">
              <div class="input-group">
                <%= search_field_tag :q, params[:q], class: 'form-control',
                                     placeholder: 'Search Digital Collections' %>
                <span class="input-group-btn">
                  <button type="submit" value="Search" class="btn btn-default">
                    <i class="fa fa-search"></i>
                  </button>
                </span>
              </div>
            </form>

          </ul>
        </div> <!-- /.navbar-collapse -->
      </div> <!-- /.container-fluid -->
    </nav>

    <div class="container-fluid">

      <% flash.each do |type, message| %>
        <div class="kq-flash alert alert-dismissable <%= bootstrap_class_for(type) %>">
          <button type="button" class="close" data-dismiss="alert"
                  aria-hidden="true">&times;</button>
          <%= message %>
        </div>
      <% end %>

      <div id="kq-page-content">
        <%= yield %>
      </div>
    </div>

    <%= render 'layouts/footer' %>

  </body>
</html>
